# tammy
def load_model(model_path):
    # TODO: Load the pretrained model
    model = ""
    return model

def aggregate_data(new_data, historical_data):
    # TODO: aggregate historical results and new blood test
    combined_data = ""
    return combined_data

def predict_aki(model, data):
    # TODO: return prediction
    prediction = model.predict(data)
    return prediction
